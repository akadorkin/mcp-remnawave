# Tools

Generated by `npm run docs:tools` — 200 tools, 95 of them available in read-only mode (✓ in the RO column).

## Fleet views

| Tool | RO | What it does |
|---|---|---|
| `fleet_overview` | ✓ | One-screen health of the whole panel: nodes (compact), counts, and consistency problems that used to need psql joins — enabled hosts whose inbound is not bound on any of their nodes, hosts without inbound/nodes, inbounds in no squad, nodes down. |
| `inbound_trace` | ✓ | Follow one inbound through everything that must line up for it to reach a client: profile → nodes binding it → squads including it → hosts pointing at it (and their nodes) → template pools those hosts are in. |
| `host_usage` | ✓ | Where a host is used: its inbound/nodes, every template pool that injects it (position, outbound tag it gets, whether it is the first member = fallback target or the only one), and which visible locations those templates serve. |
| `config_search` | ✓ | Search a string or regex across the panel: config profiles, subscription templates, hosts and nodes. |
| `pools_audit` | ✓ | Audit every template's pools the way the panel resolves them: empty pools, uuids pointing to deleted/disabled hosts, broken regexes, balancer selectors / fallbackTags / rule outbounds that point to nothing, templates no visible host uses. |

## Change safety

| Tool | RO | What it does |
|---|---|---|
| `journal_list` | ✓ | What this server changed on the panel: the write journal (newest first) with the backup file of each change |
| `backups_list` | ✓ | Backups taken before writes (profiles, templates, hosts, nodes, squads), newest first |
| `backup_restore` |  | Roll an object back to a backup made by this server: config profile, template, host(s), squad inbounds, node inbound bindings, or hosts order. |
| `api_request` |  | Escape hatch: call any panel REST route (path must start with /api/). |

## Config profiles & inbounds

| Tool | RO | What it does |
|---|---|---|
| `config_profiles_list` | ✓ | List config profiles: counts of inbounds/outbounds/rules and the nodes using each |
| `config_profile_read` | ✓ | Read a config profile without drowning in it. |
| `inbounds_list` | ✓ | List inbounds across profiles as compact rows (tag, port, protocol, REALITY dest/SNI) with the nodes that bind them, squads that include them and number of enabled hosts. |
| `config_profiles_get_computed_config` | ✓ | Computed configuration of a profile as the panel sends it to nodes (large; spilled to a file) |
| `config_profile_patch` |  | Edit a config profile surgically: a list of operations on paths, applied to a fresh copy, then diffed. |
| `config_profiles_update` |  | Replace a profile config wholesale (or rename it). |
| `config_profiles_create` |  | Create a config profile. |
| `config_profiles_delete` |  | Delete a config profile (all its inbounds go: host bindings are nulled, squad links and node bindings deleted). |
| `config_profiles_reorder` |  | Reorder config profiles; pass names/uuids in the desired order (others follow) |

## Subscription templates & pools

| Tool | RO | What it does |
|---|---|---|
| `sub_templates_list` | ✓ | List subscription templates with type and how many visible hosts (locations) render with each |
| `sub_template_read` | ✓ | Read a subscription template. |
| `sub_settings_get` | ✓ | Get global subscription settings |
| `sub_template_patch` |  | Edit one or many subscription templates with path operations (no need to resend a 160 KB template to change one line). |
| `sub_templates_update` |  | Replace a template wholesale (templateJson or encodedTemplateYaml) or rename it. |
| `pools_edit` |  | Edit explicit (uuids) host pools across all XRAY_JSON templates at once: add a host next to an anchor host wherever the anchor is listed, remove hosts from every pool, or swap one host for another in place. |

## Subscriptions

| Tool | RO | What it does |
|---|---|---|
| `subscription_fetch` | ✓ | Render a subscription exactly as a client app gets it (by User-Agent) and summarise it: size (raw and gzip), sha256, format, per-config sizes and remarks. |
| `subscriptions_list` | ✓ | List subscriptions with pagination |
| `subscriptions_get_by_id` | ✓ | Get subscription of a user by numeric user id |
| `subscriptions_get_by_username` | ✓ | Get subscription by username |
| `subscriptions_get_by_short_uuid` | ✓ | Get subscription by short UUID |
| `subscription_info` | ✓ | Public subscription info (what the client app sees) by short UUID |
| `subscriptions_get_raw_by_short_uuid` | ✓ | Raw subscription (hosts with resolved links) by short UUID |
| `subscriptions_get_connection_keys` | ✓ | Connection keys (links) of a user by numeric user id |
| `subscription_request_history_list` | ✓ | Subscription request history (paginated, filterable) |
| `subscription_request_history_stats` | ✓ | Subscription request history statistics |

## Nodes

| Tool | RO | What it does |
|---|---|---|
| `nodes_list` | ✓ | List nodes as compact rows: state, profile, inbound/host counts, users online, xray version, traffic. |
| `nodes_get` | ✓ | One node in detail: status, profile, bound inbound tags, hosts bound (visible/hidden/disabled), squads that reach it, system load, interface Mbit/s. |
| `nodes_tags_list` | ✓ | List all node tags |
| `nodes_create` |  | Create a node. |
| `nodes_update` |  | Update node fields (only the provided ones change). |
| `node_inbounds_edit` |  | Change which inbounds a node listens on, by tag: add / remove / set, keeping every other binding. |
| `nodes_delete` |  | Delete a node. |
| `nodes_enable` |  | Enable a node |
| `nodes_disable` |  | Disable a node (keeps bindings and history; hosts on it stay as they are) |
| `nodes_reset_traffic` |  | Reset traffic counter of a node |
| `nodes_restart` |  | Restart xray on a node. |
| `nodes_restart_all` |  | Restart xray on ALL nodes (every user reconnects). |
| `nodes_reorder` |  | Reorder nodes in the panel list. |
| `nodes_bulk_profile_modification` |  | Set config profile and inbounds (by tag) for several nodes at once — replaces their bindings wholesale. |
| `nodes_bulk_actions` |  | Enable / disable / restart / reset traffic on selected nodes |
| `nodes_bulk_update` |  | Bulk update properties for selected nodes |

## Hosts

| Tool | RO | What it does |
|---|---|---|
| `hosts_list` | ✓ | Find hosts. |
| `hosts_get` | ✓ | Get one host (raw panel object plus resolved inbound tag / node names) |
| `hosts_tags_list` | ✓ | List all host tags |
| `hosts_create` |  | Create a host. |
| `hosts_update` |  | Update one host (only given fields change). |
| `hosts_bulk_edit` |  | Edit many hosts at once: select by filter, then set fields, regex-replace inside text fields (remark/address/sni/host/path/serverDescription), add/remove tags, change nodes, rebind inbound. |
| `hosts_clone` |  | Clone hosts (e.g. |
| `hosts_move` |  | Move hosts to a new place in the list (before/after an anchor host, or to start/end). |
| `hosts_bulk_enable` |  | Enable hosts |
| `hosts_bulk_disable` |  | Disable hosts. |
| `hosts_bulk_delete` |  | Delete hosts (backup kept). |
| `hosts_bulk_update` |  | Panel-side bulk update: the same raw values on all given hosts in one request (no per-host diff). |
| `hosts_delete` |  | Delete one host (same checks as hosts_bulk_delete) |

## Internal squads

| Tool | RO | What it does |
|---|---|---|
| `squads_list` | ✓ | List internal squads: members, inbound count (withTags adds the inbound tags) |
| `squads_get` | ✓ | Internal squad with its inbound tags grouped by profile and the nodes those inbounds are bound on |
| `squads_accessible_nodes` | ✓ | Nodes reachable through a squad |
| `squads_create` |  | Create an internal squad with inbounds given by tag |
| `squad_inbounds_edit` |  | Add / remove inbounds (by tag) of an internal squad, keeping the rest. |
| `squads_delete` |  | Delete an internal squad (backup kept). |
| `squads_reorder` |  | Reorder internal squads (names/uuids in desired order; others follow) |
| `squads_add_users` |  | Add specific users (by numeric id) to an internal squad |
| `squads_remove_users` |  | Remove specific users (by numeric id) from an internal squad |
| `squads_add_all_users` |  | Add EVERY user of the panel to an internal squad. |
| `squads_remove_all_users` |  | Remove EVERY user from an internal squad. |

## External squads

| Tool | RO | What it does |
|---|---|---|
| `external_squads_list` | ✓ | List external squads |
| `external_squads_get` | ✓ | Get an external squad by UUID |
| `external_squads_create` |  | Create an external squad |
| `external_squads_update` |  | Update an external squad (templates, subscription settings, headers, HWID settings...) |
| `external_squads_delete` |  | Delete an external squad |
| `external_squads_add_all_users` |  | Assign EVERY user of the panel to an external squad |
| `external_squads_remove_all_users` |  | Detach EVERY user from an external squad |
| `external_squads_reorder` |  | Reorder external squads |

## Users

| Tool | RO | What it does |
|---|---|---|
| `users_list` | ✓ | List Remnawave VPN users with pagination and optional column filters (exact match). |
| `users_find` | ✓ | Find users by an exact attribute value: telegramId, email, tag or vlessUuid. |
| `users_stream` | ✓ | Cursor-based export of users, optionally filtered by status/strategy/telegramId/email/tag/externalSquadUuid. |
| `users_get` | ✓ | Get a user by numeric id |
| `users_get_by_username` | ✓ | Get a user by username |
| `users_get_by_short_uuid` | ✓ | Get a user by subscription short UUID |
| `users_tags_list` | ✓ | List all user tags |
| `users_resolve` | ✓ | Resolve a user by exactly one of: id, shortUuid, username |
| `users_accessible_nodes` | ✓ | List nodes a user can connect to (via their internal squads) |
| `users_subscription_request_history` | ✓ | Subscription fetch history (client apps, IPs, user agents) for a user |
| `users_create` |  | Create a new VPN user |
| `users_update` |  | Update an existing user (identify by id or username) |
| `users_delete` |  | Delete a user |
| `users_enable` |  | Enable a disabled user |
| `users_disable` |  | Disable a user |
| `users_revoke_subscription` |  | Revoke subscription: regenerates the short UUID and credentials (or only passwords) |
| `users_reset_traffic` |  | Reset traffic counter of a user |
| `users_extend` |  | Extend expiration date of a user by N days |
| `users_bulk_delete_by_status` |  | Delete all users with the given status |
| `users_bulk_update` |  | Bulk update fields for selected users |
| `users_bulk_reset_traffic` |  | Reset traffic for selected users |
| `users_bulk_revoke_subscription` |  | Revoke subscriptions for selected users |
| `users_bulk_delete` |  | Delete selected users |
| `users_bulk_update_squads` |  | Set internal squads for selected users |
| `users_bulk_extend_expiration` |  | Extend expiration date for selected users |
| `users_bulk_all_update` |  | Update ALL users at once |
| `users_bulk_all_reset_traffic` |  | Reset traffic counters for ALL users |
| `users_bulk_all_extend_expiration` |  | Extend expiration date for ALL users |

## HWID devices

| Tool | RO | What it does |
|---|---|---|
| `hwid_devices_list` | ✓ | List HWID devices of a user |
| `hwid_devices_list_all` | ✓ | List HWID devices across all users (paginated) |
| `hwid_stats` | ✓ | HWID device statistics |
| `hwid_top_users` | ✓ | Users with the most HWID devices |
| `hwid_device_create` |  | Register a HWID device for a user |
| `hwid_device_delete` |  | Delete one HWID device of a user |
| `hwid_devices_delete_all` |  | Delete all HWID devices of a user |

## Connections

| Tool | RO | What it does |
|---|---|---|
| `connections_by_user` | ✓ | Start a job collecting active connections (IPs) of a user across nodes. |
| `connections_by_user_result` | ✓ | Fetch the result of a connections_by_user job |
| `connections_by_node` | ✓ | Start a job collecting active connections of all users on a node. |
| `connections_by_node_result` | ✓ | Fetch the result of a connections_by_node job |
| `connections_geocheck` | ✓ | Start a geo/IP check from a node (optionally for a given ip or interface). |
| `connections_geocheck_result` | ✓ | Fetch the result of a geocheck job |
| `connections_drop` |  | Drop active connections by user ids or IP addresses on all / specific nodes |

## Bandwidth

| Tool | RO | What it does |
|---|---|---|
| `bandwidth_nodes_usage` | ✓ | Traffic per node for a period (top N nodes) |
| `bandwidth_node_users_usage` | ✓ | Top users by traffic on one node for a period |
| `bandwidth_nodes_users_usage` | ✓ | Top users by traffic across several nodes for a period |
| `bandwidth_nodes_usage_by_uuids` | ✓ | Traffic of the given nodes for a period (optionally only nodes above minTotalBytes) |
| `bandwidth_user_usage` | ✓ | Traffic of a user per node for a period |
| `bandwidth_squad_usage` | ✓ | Traffic of users in an internal squad for a period (cursor paginated) |
| `bandwidth_squad_user_usage` | ✓ | Traffic of one user inside an internal squad for a period |

## System

| Tool | RO | What it does |
|---|---|---|
| `system_stats` | ✓ | Panel statistics: users, nodes, traffic, system resources |
| `system_bandwidth_stats` | ✓ | Bandwidth statistics over standard periods |
| `system_nodes_metrics` | ✓ | Per-node metrics |
| `system_nodes_statistics` | ✓ | Nodes statistics (traffic per node over last days) |
| `system_stats_recap` | ✓ | Recap statistics |
| `system_stats_digest` | ✓ | Digest statistics for a period |
| `system_http_stats` | ✓ | HTTP request statistics of the panel |
| `system_health` | ✓ | Panel health check |
| `system_metadata` | ✓ | Panel metadata (version, build) |
| `system_configuration` | ✓ | Panel runtime configuration |
| `system_generate_x25519` | ✓ | Generate an X25519 key pair for VLESS Reality |
| `auth_status` | ✓ | Auth status of the panel |
| `system_srr_matcher` | ✓ | Test subscription response rules (SRR) against the matcher |
| `keygen_get` | ✓ | Generate a new SECRET_KEY for node configuration |

## Node plugins & shared lists

| Tool | RO | What it does |
|---|---|---|
| `node_plugins_list` | ✓ | List node plugins |
| `node_plugins_get` | ✓ | Get a node plugin by UUID |
| `node_plugins_torrent_reports` | ✓ | Torrent blocker reports (paginated) |
| `node_plugins_torrent_stats` | ✓ | Torrent blocker statistics |
| `shared_lists_list` | ✓ | List node plugin shared lists |
| `node_plugins_create` |  | Create a node plugin |
| `node_plugins_update` |  | Update a node plugin |
| `node_plugins_delete` |  | Delete a node plugin |
| `node_plugins_reorder` |  | Reorder node plugins |
| `node_plugins_clone` |  | Clone a node plugin |
| `node_plugins_sync` |  | Push a plugin to the nodes that use it |
| `node_plugins_execute` |  | Execute a plugin command (blockIps / unblockIps / recreateTables) on target nodes |
| `node_plugins_torrent_truncate` |  | Truncate all torrent blocker reports |
| `shared_lists_create` |  | Create a shared list |
| `shared_lists_update` |  | Update a shared list |
| `shared_lists_sync` |  | Push a shared list to nodes |

## Node integrations

| Tool | RO | What it does |
|---|---|---|
| `node_integrations_list` | ✓ | List node integrations |
| `node_integrations_get` | ✓ | Get a node integration by UUID |
| `node_integrations_create` |  | Create a node integration |
| `node_integrations_update` |  | Update a node integration |
| `node_integrations_delete` |  | Delete a node integration |

## Infra billing

| Tool | RO | What it does |
|---|---|---|
| `billing_providers_list` | ✓ | List infrastructure billing providers |
| `billing_provider_get` | ✓ | Get a billing provider by UUID |
| `billing_nodes_list` | ✓ | List billing nodes |
| `billing_history_list` | ✓ | List billing history (paginated) |
| `billing_provider_create` |  | Create a billing provider |
| `billing_provider_update` |  | Update a billing provider |
| `billing_provider_delete` |  | Delete a billing provider |
| `billing_node_create` |  | Attach a node to a billing provider |
| `billing_node_update` |  | Set next billing date for billing nodes |
| `billing_node_delete` |  | Delete a billing node |
| `billing_history_create` |  | Record a payment |
| `billing_history_delete` |  | Delete a payment record |

## Subscription page configs

| Tool | RO | What it does |
|---|---|---|
| `sub_page_configs_list` | ✓ | List all subscription page configurations |
| `sub_page_configs_get` | ✓ | Get a subscription page config by UUID |
| `sub_page_configs_create` |  | Create a subscription page configuration |
| `sub_page_configs_update` |  | Update a subscription page configuration |
| `sub_page_configs_delete` |  | Delete a subscription page configuration |
| `sub_page_configs_reorder` |  | Reorder subscription page configurations |
| `sub_page_configs_clone` |  | Clone a subscription page configuration |

## Snippets, settings, metadata, API tokens

| Tool | RO | What it does |
|---|---|---|
| `api_tokens_list` | ✓ | List API tokens |
| `api_tokens_scopes` | ✓ | List available API token scopes |
| `api_tokens_create` |  | Create an API token |
| `api_tokens_delete` |  | Delete an API token |
| `snippets_list` | ✓ | List configuration snippets |
| `snippets_create` |  | Create a configuration snippet |
| `snippets_update` |  | Replace the content of a snippet |
| `snippets_delete` |  | Delete a snippet by name |
| `snippets_sync` |  | Push a snippet to nodes / config profiles that use it |
| `settings_get` | ✓ | Get Remnawave panel settings |
| `settings_update` |  | Update Remnawave panel settings |
| `metadata_node_get` | ✓ | Get custom metadata of a node |
| `metadata_user_get` | ✓ | Get custom metadata of a user |
| `metadata_node_upsert` |  | Create or replace custom metadata of a node |
| `metadata_user_upsert` |  | Create or replace custom metadata of a user |
